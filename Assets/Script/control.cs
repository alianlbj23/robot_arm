using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class control : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
        // public Transform link1;
        // public Transform link2;
        // public Transform link3;
        // public Transform link4;
        // public Transform link5;
    }

    // Update is called once per frame
    void Update()
    {
        
        // link1.localRotation = Quaternion.Euler(0, 0, -90); 
        // link2.localRotation = Quaternion.Euler(0, 0, 0); 
        // link3.localRotation = Quaternion.Euler(-180, 0, 0); 
        // link4.localRotation = Quaternion.Euler(0, -90, 180); 
        // link5.localRotation = Quaternion.Euler(0, 0, 180); 
    }
}
